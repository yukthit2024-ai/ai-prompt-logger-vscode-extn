*	create a VS Code enxtension which does the following
		It has capture prompts submitted by user as well as responses generated by the AI agent.  
		It has to be written to a file with file name "prompt-and-response-yyyyMMdd.hhmmss.txt".  
		The prompt and response should be separated with some specific separator, like a horizonal lines full of '-'
*	add github action yml for CI/CD so that final output vsxi file is generated
